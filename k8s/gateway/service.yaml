apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: aiops
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: gateway-service
  type: LoadBalancer
