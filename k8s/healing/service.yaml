apiVersion: v1
kind: Service
metadata:
  name: healing-service
  namespace: aiops
spec:
  ports:
    - port: 8082
      targetPort: 8082
      name: http
  selector:
    app: healing-service
  type: ClusterIP
